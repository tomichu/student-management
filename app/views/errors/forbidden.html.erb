<div id="error">
	<h1>403 Forbidden</h1>
	<p>
		<%=
			case @exception
			when ApplicationController::IpAddressRejected
				"あなたのIPアドレス(#{request.ip})からはアクセスできません。"
			else
				'指定されたページにアクセスする権限がありません。'
			end
		%>
	</p>
</div>

