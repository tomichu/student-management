<header>
	<%= link_to 'TJ Customer Management System', :admin_root, class: 'logo-mark' %>
	<%= content_tag(:span, flash.notice, class: 'notice') if flash.notice %>
	<%= content_tag(:span, flash.alert, class: 'alert') if flash.alert %>

	<%=
		if current_administrator
			link_to 'Logout', :admin_session, method: :delete
		else
			link_to 'Login', :admin_login
		end
	%>
</header>

