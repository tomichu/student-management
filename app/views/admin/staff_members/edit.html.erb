<% @title = 'スタッフ情報を編集' %>
<h1><%= @title %></h1>

<div id="generic-form">
	<%= form_for [:admin, @staff_member ] do |f| %>
		<%= render 'form', f: f %>
		<div class="buttons">
			<%= f.submit '編集' %>
			<%= link_to 'キャンセル', :admin_staff_members %>
		</div>
	<% end %>
</div>

